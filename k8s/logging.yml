# Fluent Bit DaemonSet config for collecting Semantys logs
# Sends logs to your preferred backend (Elasticsearch, Loki, Datadog, etc.)
apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  namespace: semantys
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush        5
        Daemon       Off
        Log_Level    info
        Parsers_File parsers.conf

    [INPUT]
        Name             tail
        Path             /app/logs/*.log
        Tag              semantys.*
        Refresh_Interval 10
        Read_from_Head   True

    [FILTER]
        Name   grep
        Match  semantys.*
        Regex  log .+

    [OUTPUT]
        Name            stdout
        Match           *
        Format          json_lines

    # Uncomment for Elasticsearch:
    # [OUTPUT]
    #     Name            es
    #     Match           semantys.*
    #     Host            elasticsearch.logging.svc.cluster.local
    #     Port            9200
    #     Index           semantys-logs
    #     Type            _doc

    # Uncomment for Loki (Grafana):
    # [OUTPUT]
    #     Name            loki
    #     Match           semantys.*
    #     Host            loki.monitoring.svc.cluster.local
    #     Port            3100
    #     Labels          job=semantys,namespace=semantys

  parsers.conf: |
    [PARSER]
        Name        semantys_log
        Format      regex
        Regex       ^(?<time>[^ ]+) \| (?<level>[^ ]+) \| (?<message>.+)$
        Time_Key    time
        Time_Format %Y-%m-%dT%H:%M:%S
